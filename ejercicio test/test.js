import pareimpar from './ejercicio.js'


describe('pareimpar(num)'), () => {
    it('debería retornar impar cuando recibe 1', () => {
        expect(pareimpar(1)).toBe('impar');
    });
};

describe('pareimpar(num)'), () => {
    it('debería retornar par cuando recibe 2', () => {
        expect(pareimpar(2)).toBe('par');
    });
};

